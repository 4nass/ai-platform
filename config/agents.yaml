decomposer:
  provider: codex_cli

architect:
  provider: claude_code

backend:
  provider: claude_code

frontend:
  provider: claude_code

reviewer:
  provider: codex_cli

security:
  provider: claude_code

tests:
  provider: claude_code

documentation:
  provider: claude_code