decomposer:
  provider: claude_code

architect:
  provider: claude_code

backend:
  provider: claude_code

frontend:
  provider: claude_code

reviewer:
  provider: claude_code

security:
  provider: claude_code

tests:
  provider: claude_code

documentation:
  provider: claude_code